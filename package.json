{
  "name": "binparse",
  "version": "2.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/blacha/binparse.git"
  },
  "license": "MIT",
  "type": "module",
  "main": "./build/src/index.js",
  "types": "./build/src/index.d.ts",
  "scripts": {
    "build": "tsc --pretty",
    "build-watch": "tsc --pretty --watch",
    "lint": "eslint . --quiet --fix --ignore-path .gitignore",
    "format": "npx prettier -w .github/**/*.yml *.md",
    "test": "node --test"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  },
  "files": [
    "build/src"
  ],
  "devDependencies": {
    "@linzjs/style": "^4.0.0",
    "@types/node": "^20.5.0"
  }
}
